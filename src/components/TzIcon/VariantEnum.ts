export enum VariantEnum {
  ACCENT = 'accent',
  INVERSE_ACCENT = 'inverse-accent',
  PRIMARY = 'primary',
  INVERSE_PRIMARY = 'inverse-primary',
  SECONDARY = 'secondary',
  INVERSE_SECONDARY = 'inverse-secondary',
  NEGATIVE = 'negative',
  INVERSE_NEGATIVE = 'inverse-negative',
  DARK = 'dark',
  INVERSE_DARK = 'inverse-dark',
  LIGHT = 'light',
  INVERSE_LIGHT = 'inverse-light',
}
